/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Claude AI — local only, never commit
.claude/

# Installed skills (skills CLI) — local tooling, never commit
.agents/
skills-lock.json

# Scope & proposal documents — keep locally, not in repo
Scope and Proposal/

# Screenshots — working folder, not for repo
screenshots/

# WhatsApp / misc images dropped in root
WhatsApp Image*
